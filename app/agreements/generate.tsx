import { Placeholder } from '@/dev/Placeholder';

export default function GenerateAgreementScreen() {
  return (
    <Placeholder
      title="Generate agreement"
      milestone="M10"
      description="Long-running job. 20 per hour per IP."
    />
  );
}
